<!--
TEMPLATE — CLAUDE.md. Keep it thin. Its whole job is to import AGENTS.md so Claude Code
loads the canonical guide automatically, and to host Claude-specific overrides only.
Don't duplicate AGENTS.md content here.
-->

# CLAUDE.md

The canonical guide for working in this repo is `AGENTS.md` — imported below so Claude Code loads it automatically. Keep all agent guidance there (one source for every agent/tool); only put Claude-specific overrides in this file.

@AGENTS.md

Reminder: read `LEARNINGS.md` before starting work, and append to it when you make and correct a mistake (protocol in AGENTS.md).

{{Optional Claude-only overrides below — e.g. preferred subagents, scratchpad conventions, tool notes. Leave empty if none.}}
